import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nav-google-maps',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
